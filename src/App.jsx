import './App.css';

// JSX: sintassi con i tag, non è né una stringa né HTML.
// È chiamata JSX, ed è un’estensione della sintassi JavaScript. Utilizzata con React per descrivere l’aspetto che dovrebbe avere la UI (User Interface, o interfaccia utente). JSX potrebbe ricordare un linguaggio di template, ma usufruisce di tutta la potenza del JavaScript.
// JSX produce “elementi React”.
function App() {
  return (
    <>
      <div></div>
    </>
  );
}

export default App;
